# Implementation Plan

1. 执行仓库更新检查并加载前端开发规约。
2. 对待修改符号完成影响范围检查；GitNexus 不可用时记录契约与 grep 降级结果。
3. 删除历史上下文生成模块，清理侧栏和终端 Store 的所有调用。
4. 移除启动时重命名/重新分组已有历史项目的分支，保留缺失项目创建路径。
5. 同步成功后将所选项目 key 合并进现有持久化忽略列表。
6. 增加或调整聚焦回归测试，覆盖原始启动命令、用户分组持久化和已同步项目不再出现。
7. 更新 `CHANGELOG.md` 的 `V1.3.0`；产品功能清单仅在已有对应条目时同步调整。
8. 运行 `npx tsc --noEmit` 和相关聚焦测试，不运行用户未授权的 build/dev 命令。
9. 运行变更范围检查并汇报残余风险。
10. 统一终端字体候选与当前值的规范化规则，并增加覆盖多种字体名称形态的回归测试。
11. 在历史项目同步 Store 入口增加同步防重入守卫，并锁定回归测试。
