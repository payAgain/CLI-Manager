# Implementation

1. 新增并测试 Linux graphics policy；在 `run()` 最前应用策略并注册诊断 command。
2. 增加 Linux 隐藏启动配置、加载状态和启动 watchdog/阶段日志。
3. 扩展 settings、开发者页、i18n，并把图形诊断传递给 xterm WebGL 生命周期。
4. 扩展 distribution 信息与 updater 行为，添加 AUR PKGBUILD/wrapper 模板。
5. 更新 CHANGELOG、功能清单和相关 Trellis spec。
6. 运行 Node/TypeScript/Rust/AUR 静态验证；记录无法在当前 Windows 环境执行的 Linux 实机项。
